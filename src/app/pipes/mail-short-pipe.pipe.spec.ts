import { MailShortPipePipe } from './mail-short-pipe.pipe';

describe('MailShortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MailShortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
